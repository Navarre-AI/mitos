{
  "name": "mitos",
  "version": "0.9.1",
  "description": "Mitos, Ariadne's thread. Search sidecar for FileMaker: one field, any table, the right record. Deterministic search over the fields you choose.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "eval": "node scripts/eval.mjs",
    "postinstall": "node scripts/fetch-duckdb.mjs",
    "simulate": "node scripts/simulate.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
